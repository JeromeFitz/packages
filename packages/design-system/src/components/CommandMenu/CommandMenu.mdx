import { Story, Props, Source, Preview } from '@storybook/addon-docs'
import { Text } from '../index'
import { Default as CommandMenu } from './CommandMenu.stories'

# CommandMenu

New Component that replaces `kbar` for [`cmdk`](https://github.com/pacocoursey/cmdk).

Before we inspired heavily by ▲ Vercel's Command Menu,
and now we are even more so from the `cmdk` theme.

Toggle via `⌘K`

## Preview

<Preview>
  <CommandMenu />
</Preview>
